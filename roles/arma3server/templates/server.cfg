// Basic settings
hostname = {{ arma_server_hostname }};
password = {{ arma_server_password }};
passwordAdmin = {{ arma_server_admin_password }};
maxPlayers = {{ arma_server_max_players }};

// Security
kickDuplicate = 1;
verifySignatures = 2;
onUnsignedData = "kick (_this select 0)";
onDifferentData = "kick (_this select 0)";
onHackedData = "ban (_this select 0)";

// Voting
voteMissionPlayers = 1;
voteThreshold = {{ arma_server_vote_threshold }};
 
// Game settings
BattlEye = {{ arma_server_enable_battleye }};
disableVoN = {{ arma_server_disable_voice }};
// Range is 0-30
vonCodecQuality = {{ arma_server_voice_quality }};
persistent = {{ arma_server_enable_persistent }};
 
class Missions {};